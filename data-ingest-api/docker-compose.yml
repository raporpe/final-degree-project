version: '2'
services:
  nginx:
    image: nginx
    volumes:
    - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
    - "80:80"
    environment:
    - NGINX_PORT=80
    depends_on:
    - "api"
  api:
    build: .
    ports:
    - "3000:2000"

